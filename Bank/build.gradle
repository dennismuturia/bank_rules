apply plugin: 'java'
apply plugin: 'application'

archivesBaseName = "bank"
version = '1.0'
mainClassName = "Bank"

repositories {
  mavenCentral()
}

dependencies {
  testCompile group: 'junit', name: 'junit', version: '4.+'
}
